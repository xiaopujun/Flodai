1. 所有代码注释信息（注意是注释信息，不是代码）都使用中英文混合
2. 所有页面效果显示的内容都使用中文
3. 前端项目的类型检查使用 pnpm vcheck
4. 前端项目的所有命令都基于pnpm运行
5. 每次任务运行都要验证遵行我的指定要求，不要超出指令范围修改代码。比如我说将代码抽取到一个单独的目录，你就只需要做抽取就行。不要在抽取的过程中还去调整样式（除非是必须的）。
6. 任务执行要实事求是，不能夸大也不能偷懒
7. 每次任务的回答可以不用详细解释，只要给出任务梗概即可。（除非我明确要求详细输出任务过程细节）